# Supabase Configuration
# Get these values from your Supabase project settings at https://supabase.com/dashboard

# For local development with Supabase CLI, use:
# NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
# NEXT_PUBLIC_SUPABASE_ANON_KEY=<your-local-anon-key>

# For production, use your Supabase project URL and keys:
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (server-side only, keep secret!)
# Used for elevated database operations in Inngest functions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Supabase CLI Configuration (for local development)
SUPABASE_PROJECT_URL=https://localhost:54331
SUPABASE_ACCESS_TOKEN=your-supabase-access-token
SUPABASE_API_KEY=your-supabase-api-key

# Anthropic API Key
# Get your API key from https://console.anthropic.com/
# Used for AI-powered meal plan generation with Claude
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Inngest Configuration
# For local development, set INNGEST_DEV=1 to use local Inngest dev server
# Run `npx inngest-cli@latest dev` to start the local server
INNGEST_DEV=1
INNGEST_BASE_URL=http://127.0.0.1:8288

# For production, you'll need:
# INNGEST_EVENT_KEY=your-production-inngest-event-key
# (Do not set INNGEST_DEV in production)

# Meal Plan Test Mode (Development Only)
# Speeds up meal plan generation for faster development iteration.
# IMPORTANT: Do NOT set this in production - leave unset for production behavior.
# To unset this environment variable to test actual production code/behavior, use `unset MEAL_PLAN_TEST_MODE`.
#
# Options:
# - fixture: Instant mock data (no API calls) - for UI/styling work
# - haiku-minimal: Fast, cheap, 1 day repeated (RECOMMENDED for most dev work)
# - haiku-full: Moderate speed, full 7 days with Haiku
# - sonnet-minimal: Quality test, 1 day with Sonnet
# - production: Full Sonnet generation (default if not set)
#
# MEAL_PLAN_TEST_MODE=haiku-minimal

# Resend Email Configuration
# Get your API key from https://resend.com/
# Used to send email notifications when meal plans are ready
RESEND_API_KEY=your-resend-api-key-here
# The verified domain email to send from (e.g., notifications@yourdomain.com)
RESEND_FROM_EMAIL=notifications@yourdomain.com
NEXT_PUBLIC_APP_URL=https://your-app-url.com
